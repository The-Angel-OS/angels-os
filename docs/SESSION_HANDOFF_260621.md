# Session Handoff — 2026-06-21 (afternoon, ~6h regression sprint)

> Three fixes shipped to `main` (deploying): deep-link bounce, portal `kendev.kendev.co`, LEO reply persist. Start the new thread by **verifying these on prod**, then pick up the per-node punch list below. Model: Opus 4.8 (dialed-back tier).

## ✅ Shipped this session (pushed `b2fc37b..2a04e36`, Vercel building)
- **`1f436ce` deep-link** — `/dashboard/spaces/<spaceId>/<channelId>` no longer bounces to the root/default channel. ChatProvider seeds space from the URL + suppresses the auto-LEO jump; MultiChannelChat resolves the channel id→slug (incl. DMs) and gates URL-sync until resolved. New `deepLink.ts` parser + unit tests. **Verified live** (localhost, default + non-default channels).
- **`13eb444` portal chooser** — both choosers (DashboardSidebar + brochure PortalSwitcher) now navigate to the tenant's explicit `domain` instead of constructing `${slug}.${base}` (which doubled to `kendev.kendev.co` for the root tenant slugged `kendev`). localhost keeps slug-construction + dev port.
- **`2a04e36` LEO persist** — leo-stream now passes `tenant: tenantId` in all three ai_agent creates (placeholder / save-on-end / error). It previously relied on the `setTenantFromSpace` hook; when that hook's space lookup intermittently fails, the required-tenant validator threw → reply lost ("vanish on refresh"). **NOT verifiable locally** (hook usually succeeds on a healthy DB) — verify on PROD.

## 🔬 VERIFY FIRST (after deploy)
1. **Deep-link:** open `federation.kendev.co/dashboard/spaces/6/17` → lands on `#general`, stays (no bounce to a LEO channel).
2. **Portal chooser:** root portal links to `federation.kendev.co`, not `kendev.kendev.co`.
3. **LEO persist:** send a message, **refresh** → the reply remains. If it STILL vanishes → it's the systemic kendev node-health issue below (a revert of the URL feature would NOT have fixed it either).

---

## CORE (`C:\Dev\angels-os`)
- 🔴 **kendev node health (the #1 carryover fire).** Prod logs show `Uncaught Exception` across `/api/messages`, `/api/leo/stream`, `/api/gotify/poll`, `/api/presence-ops/online` while returning 200 → intermittent in-request query/write failures ("error initializing Payload" lineage). This is what makes `setTenantFromSpace` fail. NOT schema drift (no "does not exist"), NOT a provider fallback. → [[project_pg_connection_exhaustion]], [[project_leo_empty_response]].
- 🟠 **Ken's account locked** (failed-login lockout 11:57–11:59) — auto-unlocks, or clear `loginAttempts`/`lockUntil` on user 1.
- 🟠 **`.env.local` SSL** — points at `:5432` direct (clashes with `DATABASE_SSL=require`); repoint at the `:6432` pooler for prod-like local dev. → [[reference_local_dev_auth_runbook]].
- 🟠 **`kendev.kendev.co` → canonical** (SESSION_START #1): apex `kendev.co` + `www` + `platform.kendev.co` all resolve to the node; chooser now points at `federation.kendev.co` — flip `tenant.domain` to `platform.kendev.co` once platform is healthy (it currently 500s).
- 🟡 `importMap.js` dirty in working tree (regenerated by dev runs — `git checkout` it). `payload-types.ts` also dirty (pre-existing).
- 🟡 Daily Bread "three verses a day" quest surfacing; Merlin layout block in the Pages editor (SESSION_START #2/#3).

## MERLIN (`C:\Dev\merlin`, :3000 — media-server node, "Optimus Prime brain")
- 🔴 **Lock onto an Endeavor/Portal** — MerlinControl must INFER the endeavor from page context (free-text slug is backwards). → [[project_merlin_node_concept]].
- 🔴 **Beam up telemetry** → surfaced on the (equally untested) **MerlinControl block on its matching Endeavor on its Enterprise**. Registers via `/api/node-ops/register`. → [[project_three_body_shared_brain]].
- 🟠 **MerlinControl layout block** missing from the Pages "Add Layout" picker — add it so a Merlin page can be composed.
- 🟠 Ollama probed/advertised but not yet a real provider.

## NIMUE (`C:\Dev\nimue` — native-first Android client, v1.0.6 on both phones)
- 🔴 **Works reader "all hosed" (broken)** — last checked. Compounded by `getWorkJson` dropping Lexical richText → blank Works. → [[project_sprint_260620_open_threads]], [[project_nimue_offline_works]].
- 🔴 **Can't post** — may share root with the chat/channel routing + the LEO persist fix just shipped (Nimue hits the same Core endpoints); re-test after deploy.
- 🟠 **Image-attach to prompt fails SILENTLY** — Nimue lacks the `/api/log-ops/client-error` bridge, so failures never bubble (THE fix). → [[project_nimue_bugs_260621]].
- 🟠 Voice mic bug parked; Android notifications (local-notif vs FCM → Wear).

---
*Decision logged: Ken weighed reverting `7201f91` (the id-based-URL check-in). Conclusion: it caused the deep-link bounce (now fixed) but NOT the LEO vanish (server-side, older) — reverting loses the deep-link feature without fixing the main bug. Deploy+verify chosen instead; revert remains a fallback.*
